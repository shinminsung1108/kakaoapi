import React from 'react';
import ReactDOM from 'react-dom/client';
import Navibutton from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Navibutton />
  </React.StrictMode>
);